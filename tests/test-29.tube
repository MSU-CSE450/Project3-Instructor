// Make sure && and || will short circuit
function ExactlyTwo(int opt1, int opt2, int opt3) : int {
  return opt1 &&  opt2 && !opt3 ||
         opt1 && !opt2 &&  opt3 ||
        !opt1 &&  opt2 &&  opt3;
}
